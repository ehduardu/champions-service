import { Router } from 'express';

const internalRouter = Router();

// internalRouter.post('some/route', authentication, someRouteValidator, someControllerInstanceDotMethod);

export default internalRouter;